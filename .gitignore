node_modules/
dist/
.tmp/
.vite/
*.local
.DS_Store
# 本地模拟 GitHub Pages 子路径部署用的临时目录（把 dist 拷成 <仓库名>/ 再起静态服务）
.pagesim/

# 浏览器自动化测试产物
.playwright-cli/
# 截图脚本产出的 PNG（shot-*.png 是 shot-redesign.cjs，其余是 shot-landing /
# shot-trend / e2e-* 产出的复核图）。这些都是本地跑一次就变的一次性产物，
# 不入库。用具体前缀而不是 *.png，避免误伤将来真的要提交的图。
shot-*.png
trend-*.png
landing-*.png
trainer-*.png
e2e-*.cjs
# 例外：这些功能测试断言的是产品契约（取值口径、坐标映射、落点优先级），
# 是长期有效的回归网，不是一次性调试脚本。
# 注意必须在 e2e-*.cjs 之后用 ! 逐条排除才生效——gitignore 是顺序求值的。
!e2e-trend.cjs
!e2e-landing.cjs
!e2e-pwa.cjs
# 补漏：e2e-regression 是断言最多的一个套件（45 项），此前一直被这条通配吃掉，
# 等于最重要的回归网反而没进版本库。
!e2e-regression.cjs
!e2e-plan-loc.cjs
!shot-plan-result.cjs
# diag-*.cjs 是诊断脚本，输出的「结论」段有长期价值（记录数据事实），
# 保留入库；e2e-*.cjs 之外的调试脚本仍默认忽略。
!diag-loc.cjs
# 帧分析脚本与临时渲染结果（排查动作图问题时用）
.poses/
cmp-*.png
big-*.png

# 动作演示媒体（1324 个 GIF + 1324 个缩略图，约 138 MB）。
# 不入库的原因：二进制文件一旦提交就会永久留在 git 历史里，
# 且它们是数据集的可再生产物，没必要让每次 clone 都背上 138 MB。
# 拉取方式：npm run media:fetch（先 npm run dataset:fetch 拿清单）。
# 脚本可重复执行、断点续传，只补缺失的文件。
public/media/

# 上面这些审计/校验脚本是本项目自己的工具，不应被 e2e-*.cjs 规则误伤
!audit-product.cjs
!verify-fix.cjs
!verify-media.cjs
!verify-coaching.cjs
# verify-coaching 守的是「常见错误」那 3 条提示的覆盖质量。
# 2026-09-22 发现：实现按「生成顺序」取前 3 条，导致通用提示挤掉专属提示 ——
# 负重+下肢只有 22.2% 拿到「膝内扣」，而这是完全静默的（界面照样 3 条，只是内容不对）。
# 改成按具体度排序后升到 99.4%。这个套件逐类全量扫描，防止排序再退化。
!shot-redesign.cjs
# verify-backlog 核验评审报告「待办清单」的实际状态，逐条在运行中的应用上取证。
# 报告状态与代码一旦再次脱节，这个脚本是唯一能自动发现的网。
!verify-backlog.cjs
# diag-history-sort 是排查排序问题时写的，保留可复现当时的诊断过程
!diag-history-sort.cjs

# 三端适配的回归网。断言的是「契约」而不是像素值：
#   导航按钮必须全部在视口内、可点击、点击后真的切页；各页面不得横向溢出。
# 之所以必须有它：上一版的 .app-nav 带 overflow-x:auto，
# 按钮滚出可视区时 document.scrollWidth 完全不变（文档级检查全绿），
# 但用户在 320px 下根本看不到「浏览」按钮——只有逐按钮量 rect 才能发现。
!e2e-responsive.cjs
# 响应式审计与截图脚本：数字口径的复核 + 人工看图用
!audit-responsive.cjs
!audit-header.cjs
!shot-responsive.cjs

# 三端截图产物（跑一次变一次，不入库）
responsive-shots/

# e2e-equipment 断言的是「生成结果里的器械 ⊆ 用户勾选的 ∪ {自重}」，
# 这是用户报过的原始问题（在家生成了没有的器械），必须长期守着。
!e2e-equipment.cjs
# diag-loc-tag 记录场地标签的三态口径（在家 / 健身房 / 自重），
# 防止自重动作在健身房计划里被重新标成「在家」。
!diag-loc-tag.cjs

# 动作分解播放器的回归网。
# 断言的不是「元素存在」，而是读 canvas 像素确认每一帧画面真的不同 ——
# 这是唯一能证明「GIF 解码 + 帧合成」正确的方式（解码器用错会得到全透明）。
# 其中「步进时画面每次都变」这条尤其关键：数据集每个循环里夹着 1～3 处长定格，
# 早先直接对帧号 ±1，用户按「下一帧」有大概率停在几乎相同的画面上，会以为按钮坏了。
!e2e-player.cjs
# e2e 脚本的公共引导（定位 playwright 和 chromium）。
# 路径不能硬编码 —— playwright 换版本时目录名和文件名都会变，
# 复制到多个脚本里就意味着一处失效、多处报错。
!e2e-setup.cjs
# 播放器三端截图脚本，人工复核布局用
!shot-player.cjs

# 播放器截图产物（跑一次变一次，不入库）
player-shots/

# preview / dev 服务器的启动日志，本地调试用
.preview.log

# GIF 帧结构分析脚本。用于查证「动图为什么看着卡」这类问题：
# 逐帧读出 GCE 里的原始延时值，统计帧率与节奏模式。
# 上游素材若更换，这是唯一能快速复核「素材本身有多少帧、什么节奏」的工具。
!analyze-gif.cjs
!analyze-gif-stats.cjs

# 统一测试入口 —— package.json 的 test 脚本指向它，CI 也要跑，
# 必须能 checkout 到，否则 npm test 直接找不到文件。
!e2e-run-all.cjs

# e2e-optimize.cjs 测「导入/导出 JSON」时落下的临时文件
# （.tmp-bad.json 是故意写坏的那份，用来验证解析失败不崩）。
# 每次跑测试都会重新生成，不该进版本库。
# 用 .tmp-* 而不是只列 .json：临时文件不止一种后缀
# （提交信息草稿、诊断脚本都叫 .tmp-xxx），只拦 .json 会漏。
.tmp-*

# 计划向导套件。此前一直被 e2e-*.cjs 通配吃掉而没入库，
# 但它断言的是「生成器按肌群/器械/场地怎么挑动作」这条核心行为，值得留下。
# 顺带：它原先硬编码 5173，已改为读 BASE（默认值不变）。
!e2e-wizard.cjs

# 补两个漏网的套件（2026-09-22 发现）。
# e2e-optimize 有 543 行断言，测备份导入导出、计时器时间戳、偏好持久化、
# 类型化输入、截断存档、收藏、弹窗焦点管理 —— 这么大的回归网被通配吃掉，
# 等于白写；e2e-redesign 测改版后的「连加 3 个动作」链路。
# 两个都被 e2e-run-all.cjs 的 SUITES 列着，但 fresh clone 下文件不存在、
# 只能被跳过 —— 也就是说 CI 上一直在跑 12 个而不是 14 个。
# 入库后 SUITES 才是诚实的。
!e2e-optimize.cjs
!e2e-redesign.cjs

# 数据集原始清单（17MB）。用 npm run dataset:fetch 重新拉取，
# name_zh.json 只有 80KB，保留入库以便查证中文名映射。
scripts/exercises.json
scripts/failed.json
